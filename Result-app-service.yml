apiVersion: v1
kind: Service
metadata:
  name: result-app-service
  labels:
    name: result-app-service
    app: example-voting-app
spec:
  ports:
    - targetPort: 80
      port: 80
  selector:
    name: result-app-pod
    app: example-voting-app
  type: LoadBalancer
